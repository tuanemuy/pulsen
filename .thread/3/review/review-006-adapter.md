# レビュー 006 — Adapter / Ports

### Adapter / Ports（スコープラウンド: HOOKS.md:28 の修正）

#### Blockers

なし

#### Warnings

なし

#### W-001（ラウンド005）の解消判定

**解消している。**

修正後の文が指す行を実際に数えた（`crates/pulsen-conformance/HOOKS.md` の「環境で走らなくなりうる行」表 = `:36`〜`:57`）。

| ID | 表に現れる行 | 判定列が持つ能力 |
|---|---|---|
| 003 | `:44` のみ | `agent_command`（テスト用エージェント） |
| 005 | 現れない | — |
| 010 | 現れない | — |
| 013 | `:46` / `:47` | `:47` が `live_execution_unit` / `detached_execution_unit` |
| 015 | `:46` / `:47` | 同上 |
| 016 | `:46` / `:48` | `:48` が `orphaned_execution_unit` |

W-001 が突いたのは「その行にだけ現れる」の **だけ** が、表全体で読んでも（013 / 015 / 016 が2行に現れる）スキップ許容条件の行に限って読んでも（003 の `:44` 自体がフィクスチャ行）破れる点だった。修正文は「だけ」を落として **存在の主張**（その能力を判定に持つ行として現れる）に変え、重複を括弧で明示的に引き受けている。したがって

- **表全体で読む場合**: 主張は「その能力を判定に持つ行として現れる」だけなので、013 / 015 / 016 が2行に現れることは矛盾しない。括弧がその2行目を正しく `:46` と説明する。
- **スキップ許容条件の行に限って読む場合**: 前半の「005 / 010 は…この表に現れず」と同じ「この表」＝表全体の読みで一貫し、003 の `:44` を除外する必要が生じない。

どちらの読みでも破れない。

括弧の内容も実装で裏が取れる。`crates/pulsen/tests/conformance_process_controller.rs:561` の `observation_allowed_skips()` は `ExecutionUnitCapability::ProgramMissing`（＝フィクスチャの実行ファイルが無い）で **空の集合** を返す（`:570`）。「そちらはスキップを許容する条件ではない」は宣言の実体そのものである。013 / 015 は `EXECUTION_UNIT_CASES`（`:491`）、016 は `PARTIAL_TERMINATION_CASES`（`:499`）に入り、`:47` / `:48` の判定列と一致する。

#### 「主語を明示した」理由の妥当性

**妥当。**

提案文（主語なし）の括弧は、直前に並ぶ主語の列（003 / 013 / 015 / 016）のうちどこまでを受けるかが構文上決まらない。003 は表に `:44` の1行しか持たず、その `:44` 自身がフィクスチャの実行ファイル（`examples/agent_probe`）を要する行なので、003 に「フィクスチャの実行ファイルを要する行にも**重ねて**現れる」を掛けると偽になる。`crates/pulsen-conformance/src/process_controller.rs:319` で TC-003 が `require!(harness.launch_spec(...))` を踏むため、003 の依存先が `:44` の `agent_command` であることも確認した。主語を `013 / 015 / 016` に限定する変更は、この偽を構文レベルで閉じている。

#### 新たな食い違いの有無

いずれも**無し**。

- **`:59` の語彙との整合**: 修正文は「スキップを許容する条件ではない」と書き、`:59` の「この表は**スキップを許容する条件**の一覧である」と同一語彙になっている。`:59` が「フィクスチャの実行ファイルが無い場合…は、ここでいう『前提を作れない環境』には当たらない」と定めた内容と、括弧の主張は同じことを別方向から述べており矛盾しない。
- **`未測定` の規律**: 差分は散文1行のみで、表の右3列（`:46` / `:47` / `:48` は `未測定`）に触れていない。`:32` の規律にも `:30` の出典宣言にも影響しない。
- **正本の主語がフック水準に保たれているか**: 修正文は行の同定先を「**判定**列」と名指しており、`:61` が「文言もフック水準（`ハーネスが … を提供するか`）になる」と定めた列と一致する。前段の能力の言い換え（実行単位を作れる／その一部だけを終了させられる）は「前提を作れない環境」列の水準だが、文はそれを判定列へ写像する形になっており、`:61` の読み方の指示（`:47` / `:48` は括弧で読む）と衝突しない。
- **`:376` の正本宣言との整合**: 表本体は無変更で、散文だけが表の実体に追随した。ラウンド2 W-005 / ラウンド3 W-004 で直してきた正本性の方向と同じ側の修正である。

#### スコープ判定

**スコープ内。**

`.thread/3/plan.md:114` が本スライスの成果物として「`HOOKS.md` は冒頭の対象ポート数・総行数・区分別件数表と**「環境で走らなくなりうる行」の表を更新**し」と明記している。`:28` はその表の直前に置かれた読み方の説明で、表と一体の記述である。`:75` が「スキップ許容集合は環境の能力から実行時に決める」「判定の正本は … `observation_allowed_skips`」と定めた内容とも整合し、宣言の正本と散文の対応を揃える修正はこの受け入れ基準の内側にある。`.thread/3/review/triage.md` に本件と重なる `wont-fix` / `defer` は無い（`wont-fix` は W-018 の `plan.md:53` 1件のみ）。

#### 確認したが指摘に当たらない事項

担当者の報告は「提案から主語を明示した点だけ変えた」だが、実際の差分には他に2点ある。いずれも**改善方向**なので指摘としては挙げない。

- 提案の `:46` という行番号参照を「フィクスチャの実行ファイルを要する行」という記述的な指示に置き換えている。`:61` が行を `TC-port-exclusive-lock-002 / 003 / 004 / 005` や「実行単位を要する TC-port-process-controller の2行」と記述的に指す規約を持つため、こちらのほうがファイル内で一貫し、行がずれても腐らない。
- 提案の「スキップ許容条件」を「スキップを許容する条件」に伸ばし、`:59` の表記に揃えている。

提案の「**それぞれの**能力」が「**その**能力」になった点も差分に含まれる。主語が3系統（003 / 013・015 / 016）に分かれる文なので `それぞれ` のほうが配分読みは明示的だが、`A は X を、B は Y を要するため` という並列構造が配分読みを保証しており、続く括弧が 013 / 015 / 016 を名指しで再掲するため誤読の余地は残らない。文体の選択の範囲と判断する。

#### カバレッジ

- 確認: `crates/pulsen-conformance/HOOKS.md`（`:26`〜`:32` の散文、`:34`〜`:57` の表、`:59` / `:61` の読み方の規定）
- 裏取りに参照（無変更）: `crates/pulsen/tests/conformance_process_controller.rs:491-577`、`crates/pulsen-conformance/src/process_controller.rs:315-331`
- 契約照合: `.thread/3/plan.md:75,114`、`.thread/3/review/triage.md`
- スキップ: なし（前ラウンドで PR 全体を確認済み。今回は修正差分に限定）
