# Inventory — usecase

生成元: spec/usecases/・spec/flows/（最終同期: 2026-08-15）

| ID | 要素 | 定義場所 | 実装されるべき振る舞いの要点 |
|----|------|---------|------------------------------|
| UC-task-001 | RegisterTask(add) | spec/usecases/task.md#registertaskadd | ロック取得→`WorkflowRef::parse`→`WorkflowStore::load`（`NotFound`/`Io`は解決を試みたパスを添えて入力エラー、パースエラーは`Parse{error,resolved_from}`の解決先パスを位置・原因とともに表示する）→`display_name`決定（`NameError`は入力エラー）→対象検証（`validate_repo`、`base`省略時は`head_branch`解決、`DetachedHead`/`EmptyRepository`は`--base`明示を案内、`branch_exists`falseでエラー、`TargetError::Failed`は実行環境エラー）→`RegistrationValidator::validate`（`RegistrationError`全件まとめて表示、未定義エージェント参照は定義済み名一覧を添える。`status`を持たない`UnknownAgent`/`InvalidAgentDefinition`は同値の重複を1件にまとめる）→`TaskIdGenerator::generate`→`Task::register`→`TaskRepository::create`（`Conflict`はID再発行し1回だけ再試行、再衝突はエラー）→タスクID表示し0終了。ロック競合は実行環境エラーで非0。いずれのエラーでもタスクは作られない |
| UC-task-002 | RetryTask(retry) | spec/usecases/task.md#retrytaskretry | ロック取得→`find`（`NotFound`/`Corrupt`/`Archived`はエラー）→`Intact`は`task.retry(now)`、`SnapshotUnreadable`は`degraded.retry(now)`を受理し`snapshot_warning=true`で修復要と警告→stopped以外への retry は`NotStopped`エラー（failed=自動リトライ案内、pending=既に実行待ち、completed=次tickが遷移、launching/running=先にabort案内）→`save`/`save_degraded`→0終了 |
| UC-task-003 | SetTaskStatus(set-status) | spec/usecases/task.md#settaskstatusset-status | ロック取得→`find`（`NotFound`/`Corrupt`/`Archived`はエラー、`SnapshotUnreadable`は遷移先検証にスナップショットが要るため拒否）→`task.set_status(status, now)`（`Active`は先にabort案内エラー、`UnknownStatus`は定義済みステータス一覧を添えてエラー）→`save`→0終了。出力に`from`/`to`を含む |
| UC-task-004 | ListTasks(ls) | spec/usecases/task.md#listtasksls | `list_active`（`all`なら`list_archived`も）→`Corrupt`は`unreadable`へ分離、`SnapshotUnreadable`は行に含め`snapshot_unreadable`を立てる（絞り込み対象にもなる）→`status`と`state`はAND絞り込み、`all`は対象集合の拡張（拡張後に絞り込み適用）→`--state`が固定6値以外は有効値一覧を添えて非0→ロックは取得しない（読み取り専用）、`list_active`/`list_archived`のIoは実行環境エラーで非0。`status`はユーザー定義語彙のため検証せず、未知の値は該当0件になる |
| UC-task-005 | ShowTask(show) | spec/usecases/task.md#showtaskshow | `find`（tasks→archiveの順、`NotFound`はエラー、`Corrupt`はパスとエラー内容を表示し非0）→`Intact`はスナップショット由来項目（`defined_statuses`・`limits.retry`）を含めて構成、`SnapshotUnreadable`は読める項目を全表示しつつ`snapshot_error`をSome・`defined_statuses`をNone・`limits.retry`を`Unknown`として0で表示→`attempt`は`RunStore::attempt_exists`で存在確認（false→`run_dir_exists:false`）し、存在すれば`read_exit`で補完（いずれもエラーにせず注記表示で継続）→ロックは取得しない。`limits.retry`はWaitステータスで`NotApplicable`、judge/spawnの上限はスナップショット非依存で常にconfig由来の値を併記する |
| UC-execution-001 | 共通手続き: 凍結の確定と通知(notify) | spec/usecases/execution.md#共通手続き-凍結の確定と通知notify | stopped記録は遷移関数（`fail_run`/`record_spawn_failure`/`record_spawn_failure_in_place`/`record_judge_failure`/`record_tool_failure`/`abort`）が`notified_at:None`で`save`済みであることが前提→`config.notify_cmd`がNoneなら何もしない（`notified_at`は書かない。後から定義されればcatch-up通知）→`NotificationService::notify_env`→`CommandRunner::run(notify_cmd, env, NOTIFY_TIMEOUT)`→`NotificationService::interpret_notify_completion(completion)`で成否を解釈し、`Delivered`のときのみ`mark_notified(now)`→`save`/`save_degraded`、`Failed{cause:NotifyFailureCause}`は何もしない（次tickが再通知、at-least-once、二重通知は許容）。失敗の報告先は経路ごとに異なり、Tickは`errors`に`NotifyFailed{task_id,cause}`を積み、AbortTaskは`notify_warning`に載せる（共通手続きが固定するのは成否の判定経路まで）。文言は表示層が`cause`から組み立てる |
| UC-execution-002 | Tick(tick) | spec/usecases/execution.md#ticktick | `ExclusiveLock::try_acquire`、`None`はスキップ表示し0終了（cronでアラートにしない）、`Err(LockError::Failed)`は実行環境エラーで非0→`list_active`（Ioは実行環境エラーで非0・状態変更なし）→各エントリを分岐：`Corrupt`は報告のみ、`SnapshotUnreadable`は定義依存判断をスキップして報告（`Stopped{notified_at:None}`のみ共通notify実行）、`Intact`は実行状態で分岐（Pending/Failedは動作種別でWait何もしない/AgentRunは手続きA/Cleanupは手続きB、Launchingは手続きC、Runningは手続きD、Completedは`task.advance(now)`→`save`で`TransitionError`は`Transition`として報告してスキップ、Stoppedは`notified_at`Noneなら共通notify）→`run_retention`設定時は手続きE→サマリー（launched/confirmed_running/judged/transitioned/skipped_back/frozen/notified/archived/errors/gc_deleted/gc_errors の11フィールド）表示し0終了。タスクファイルへの書き込みを行った経路は必ずいずれかのフィールドを埋める（記録すべきことが1つも起きなかったtickのみ「処理対象のタスクはありませんでした」）。1タスクの処理失敗は`errors`に記録し残りは続行。`errors`は完成文言を持たない`TickIssue`の直和型（`CorruptTaskFile{path,message}`/`SnapshotUnreadable`/`MissingCurrentAttempt`/`MissingProcessIdent`/`MissingWorkspace`/`Transition{error:TransitionError}`/`RunFileUnreadable`/`InconsistentRunFiles`/`WorktreeCreateFailed`/`CommandExpansionFailed`/`MarkerWriteFailed`/`PrepareAttemptFailed`/`SpawnFailed`/`SpawnNotObserved`/`ObservationFailed`/`KillFailed`/`RemnantsUnhandled{remnants:RemnantsLeft}`/`JudgeFailed`/`RunFailed{cause:RunFailureCause}`/`NotifyFailed{cause:NotifyFailureCause}`/`SaveFailed`）で、各変種は必要なフィールドだけを持ち、文言はCLI層が組み立てる。共通notifyが`Failed{cause}`を返した場合は`errors`に`NotifyFailed`を積む。複数リソースにまたがる列（launching記録→`prepare_attempt`→spawn、worktree削除→archive、stopped記録→notify→`mark_notified`）は原子化せず、途中失敗・クラッシュ時の残存状態は次tickの冪等な再導出とat-least-onceで回復する |
| UC-execution-003 | 手続きA: 起動(Pending/Failed×AgentRun) | spec/usecases/execution.md#手続きa-起動pending--failed--agentrun | 順序は「worktree確保→テンプレート展開→launching記録→spawn」（ADR-016）。`workspace`None時は`WorkspacePlanner::derive`→`WorktreeManager::create`（自タスク残骸に冪等）、失敗は`record_tool_failure(ToolFailureKind::WorktreeCreate)`→`save`→（Stoppedならnotify）→終了、成功は`confirm_workspace`→`save`。テンプレート展開は同期処理で、実効エージェント名解決失敗・`RawAgentDefinition`不在・`parse`失敗（`AgentDefError`）・`render_input`失敗（`MissingSkillInput`）・`build_command_line`失敗（`ExpansionError`）はいずれも展開失敗として`record_spawn_failure_in_place`→`save`→（Stoppedならnotify）→終了。`record_launching(state_root, now)`→`save`（復旧の起点）。`RunStore::prepare_attempt`失敗は報告のみ（猶予経路が分類）。`ProcessController::spawn_wrapper`の同期エラーも状態を変更しない（猶予経路が分類） |
| UC-execution-004 | 手続きB: 終端処理(Pending/Failed×Cleanup) | spec/usecases/execution.md#手続きb-終端処理pending--failed--cleanup | `WorktreeManager::remove`（パスは`task.workspace`がSomeならその値、Noneでも`WorkspacePlanner::derive`の決定的導出パスを使い孤児worktreeを生まない）、`AlreadyAbsent`は成功、失敗は`record_tool_failure(ToolFailureKind::WorktreeRemove)`→`save`→（Stoppedならnotify）→終了。`TaskRepository::archive(id)`、失敗は`record_tool_failure(ToolFailureKind::ArchiveMove)`→`save`→（Stoppedならnotify）→終了。成功は`archived`に記録し走査対象から外れる（runディレクトリ・ブランチは残る）。リトライ上限は`effective_retry_limit`（Cleanupは常に組み込みデフォルト2、ADR-014）。worktree削除成功→archive失敗の場合、次tickは`remove`が`AlreadyAbsent`を返しarchiveから再開する（冪等） |
| UC-execution-005 | 手続きC: spawn確認(Launching) | spec/usecases/execution.md#手続きc-spawn確認launching | 冒頭で`current_attempt`Noneは不変条件2の破れとして`MissingCurrentAttempt`を報告しスキップ。`run_dir`から`read_pid_file`/`read_starttime`（ディレクトリ不在はNone、`RunFileError`は報告してスキップ）。破損runファイル継続時はtickスキップ・abort拒否が続きlaunchingのまま滞留（復旧は人間による当該runファイルの削除）。`LaunchingClassifier::classify`：`ConfirmRunning`→`confirm_running`→`save`、`KeepWaiting`は何もしない、`SuspectSpawnFailure`→`write_invalidation_marker`（`Err(Io)`は状態を変更せず報告してスキップ、成功なら再読→`classify_recheck`：`ConfirmRunning`→`confirm_running`→`save`、`SpawnFailed`→`record_spawn_failure`→`save`→（Stoppedならnotify）。猶予超過で確定したspawn失敗は`SpawnNotObserved`として報告し（カウンタを消費し凍結しうる）、`spawn_wrapper`の同期エラー`SpawnFailed`とは別の分類にする）、`Err(InconsistentRunFiles)`は報告してスキップ（次tickで再観測） |
| UC-execution-006 | 手続きD: 観測・判定(Running) | spec/usecases/execution.md#手続きd-観測判定running | 冒頭で`current_attempt`/`process`Noneは不変条件2〜3の破れとしてそれぞれ`MissingCurrentAttempt`/`MissingProcessIdent`を報告しスキップ。`read_exit(run_dir)`。exit Someなら判定（2段規則の1段目はここで`RunningDecision::Judge(exit)`として値にする）：`judge`未定義は`default_judgement(exit)`が返す`DefaultJudgement`(Completed/Failed)を`JudgeOutcome`へ埋め込む、`judge`定義ありは`task.workspace`がNoneなら不変条件4の破れとして`MissingWorkspace`を報告しスキップ（判定コマンドは起動せず書き込みも行わない）、Someなら`judge_env`→`CommandRunner::run(judge, env, judge_timeout)`→`interpret_judge_completion`（`JudgeFailure`は`record_judge_failure`→`save`→（Stoppedならnotify）→終了）。Completedは`complete_run`→`save`（遷移は次tick）。Skippedは`skip_run`→`save`（タスクステータス不変・pending復帰、ADR-008）。Failedは`fail_run(effective_retry_limit)`→`save`→（Stoppedならnotify）。exit Noneなら生存観測：`starttime_of(pid)`（`Err(Io)`は報告してスキップ）→`IdentityCheck::check`→`RunningClassifier::classify_alive`（返り値は`AliveDecision`。`RunningDecision`へ合流させてから網羅`match`で分岐する）：`KeepRunning`何もしない、`KillOnTimeout`は`kill`成功で`fail_run`→`save`→（Stoppedならnotify）・失敗は状態変更せず報告、`DiedWithoutExit`は`try_kill_remnants`（結果は報告のみ）→`fail_run`→`save`→（Stoppedならnotify） |
| UC-execution-007 | 手続きE: runディレクトリのgc(run_retention設定時のみ) | spec/usecases/execution.md#手続きe-runディレクトリのgcrun_retention設定時のみ | `RunStore::list_runs`→保護マップを**各タスクの処理後の状態**（手順3〜7適用後）から構築（処理前から作ると同tickで凍結したタスクの調査材料を同tickのgcが削除しうる）：`Corrupt`はpathのファイル名主部をキーに`AllProtected`、stopped（Degraded含む、当tick凍結分含む）は`AllProtected`、その他現役は`ActiveCurrent(attempt番号)`、当tickアーカイブ完了・runsにあり現役にないものは`Unprotected`、当tickの`save`に失敗したタスクは`AllProtected`（保守側に倒す）。`GcPolicy::plan`→各`(dir, attempt)`を`delete_attempt`（失敗はスキップし`gc_errors`に報告、カウンタ消費・stopped化なし）→空になった親を`remove_task_dir_if_empty`。結果をサマリーに含める。`list_runs`のIoはgcのみ中止して`gc_errors`に報告し、タスク処理は完了しているためtick自体は0 |
| UC-execution-008 | AbortTask(abort) | spec/usecases/execution.md#aborttaskabort | 分岐は実行状態ラベルでなくkill対象の有無で決まる。ロック取得→`find`（`NotFound`/`Archived`/`Corrupt`はエラー、`SnapshotUnreadable`は通常どおり進む）。既にStoppedは何も変更せず0（冪等成功）。kill対象同定：Runningは`process`（Some、不変条件3）から`starttime_of(pid)`（`Err(Io)`は状態変更せず非0・再実行案内）→`IdentityCheck::check`、Aliveは`kill(kill_ident)`（失敗は状態変更せず非0、成功は`killed=true`）、Deadはkillせず進む。Launchingは`read_pid_file`/`read_starttime`（runディレクトリ不在なら`write_invalidation_marker`がディレクトリごと作成）、pidありなら照合付きkill、pidなしは`write_invalidation_marker`→再読（pidあり→照合付きkill、依然pidなしはkillせず進む）。読み取りの`RunFileError`・「pidありstarttimeなし」・`write_invalidation_marker`失敗はいずれも状態を変更せず非0（再実行案内）。それ以外（Pending/Failed/Completed）はプロセス操作なしで進む。`task.abort(now)`/`degraded.abort(now)`→`save`/`save_degraded`→共通notify実行（失敗しても0、`notify_warning`表示）→結果表示し0。Runningで`current_attempt`/`process`がNone等の不変条件の破れ（手動修復由来）は観測前に検出し、状態を変更せず非0でタスクファイルの修復を案内（Tickが`MissingCurrentAttempt`/`MissingProcessIdent`を報告してスキップするのと同じ原則）。`save`/`save_degraded`失敗は非0でabort再実行を案内（kill成功→save失敗の残存は再実行時に観測が`Dead`を返し記録のみで完了する） |
| UC-execution-009 | RunWrapper(wrapper内部コマンド) | spec/usecases/execution.md#runwrapperwrapper-内部コマンド | 利用者向けでなくヘルプに出さない、configは読まない。`own_identity()`→`WrapperIdentity{pid, kill_ident, starttime}`。`write_starttime(run_dir, starttime)`を必ずpidより先に書く（requirements§4.1）。`write_pid_file(run_dir, {pid, kill_ident})`。`marker_exists(run_dir)`がtrueならエージェントを起動せず終了（遅延起動排除）、`Err(Io)`も起動せず終了（安全側に倒し、次tickが「exitなし・プロセス死亡」としてfailed分類）。`run_agent(agent_cmd, workspace, stdout_log, stderr_log)`→`ExitCode`（起動不能127/126、シグナル死128+n、リダイレクト不能126に符号化）。`write_exit(run_dir, exit)`→終了。ステップ1〜3・6の書き込み失敗は何も書き残さず終了（観測されない＝猶予経路またはプロセス死亡としてtickが分類）。引数の不正（直列化の破れ）は何も書かず非0終了（猶予経路がspawn失敗として分類）。終了コードはラッパー自身が責務を果たせたかを表す：エージェントを実行した場合とマーカーにより起動しなかった場合は0、同定情報を何も残せずに終えた場合と起動引数が不正な場合は非0。エージェントの終了コードは伝播しない（結末はrunディレクトリの`exit`ファイルだけが持つ記録） |
| UC-flow-001 | F1: タスクの実行状態ライフサイクル | spec/flows/index.md#f1-タスクの実行状態ライフサイクル | RegisterTaskでPending生成が起点。Pending/Failed→Launching（手続きA起動）／→Failed（worktree作成・削除・アーカイブ移動失敗、`record_tool_failure`、ADR-012）／→状態不変カウンタのみ（テンプレート展開失敗、`record_spawn_failure_in_place`、ADR-016）。Launching→Running（pid取込、`confirm_running`でspawn_fail_countリセット）／→Pending（猶予超過・マーカー・再確認、`record_spawn_failure`）。Running→Completed（判定completed、`complete_run`でattempt/judgeリセット）／→Pending（判定skipped、`skip_run`、タスクステータス不変ADR-008）／→Failed（判定failed/timeout kill/プロセス死亡、`fail_run`）／→Runningカウンタのみ（判定失敗、`record_judge_failure`）。Completed→Pending（`advance`でタスクステータスがnextへ、1タスク1tick1ステップ）。各状態→Stopped（上限超過3経路+abortの計4経路）。Stopped→Pending（retry/set-status、カウンタ全リセット）。Pending/Failed/Completed/Stopped→Pending任意ステータス（set-status手動遷移）。全状態に脱出遷移または明示された滞留の意味があり到達不能・行き止まりがないことを検証する（例外滞留はCorrupt/SnapshotUnreadableの修復待ちとLaunching×runファイル破損継続の2つのみ） |
| UC-flow-002 | F2: attempt(2フェーズ起動→観測→判定) | spec/flows/index.md#f2-attempt2フェーズ起動--観測--判定 | tick手続きAの`record_launching`でattempt採番が起点。runディレクトリ作成→デタッチspawn（tick）→starttime→pidの順で書き込み（wrapper、requirements§4.1）→マーカー確認（あれば未起動終了）→エージェント実行（cwd=worktree、ログリダイレクト）→exit書き込み（wrapper）→pid観測→running取込→exit観測→判定（デフォルト/判定コマンド、tick）。終端はcompleted/skipped/failed確定、またはspawn失敗（pidが現れずpendingへ）、判定不能の上限超過（stopped）、abortによる中断（Runningは照合付きkill、Launchingはマーカー無効化→stopped）。二重起動はattempt別ディレクトリ+マーカー順序プロトコル（wrapper「pid後にマーカー確認」×tick/abort「マーカー後にpid再確認」）で排除、PID再利用はstarttime照合（§6.2）で誤kill・誤生存判定を防ぐ |
| UC-flow-003 | F3: stopped通知(at-least-once) | spec/flows/index.md#f3-stopped-通知at-least-once | stopped確定（上限超過3経路+abortの計4経路）が起点。stopped記録（`notified_at:None`）→notify_cmd実行→成功時のみ`mark_notified`（確定させたtick/AbortTaskが主体）。`notified_at`のないstoppedは以降の全tickが検出して再通知（スナップショット破損タスクも対象、通知は定義非依存）。終端は`notified_at`の記録、またはretry/set-statusによるstopped離脱（未通知のまま離脱してよい）。notify_cmd未定義なら未通知のまま保持（catch-up通知の意図）。実行失敗・timeout（NOTIFY_TIMEOUT、ADR-018）・クラッシュいずれも「notified_atなし」が残り再通知される（二重通知は許容） |
| UC-flow-004 | F4: worktreeの生涯 | spec/flows/index.md#f4-worktree-の生涯 | 最初のAgentRun起動（tick手続きA）での`WorkspacePlanner::derive`+`WorktreeManager::create`が起点。作成（tick）→全ステータスで同一worktreeを実行対象に使用（wrapperがcwdに、リトライ間で内容は引き継がれツールはリセットしない）→削除（tick手続きB、Cleanupステータスのみ）。終端は`WorktreeManager::remove`（`AlreadyAbsent`=達成済み）。stopped・進行中タスクのworktreeは削除されない（調査材料保護）。作成失敗=failed経路（リトライ→stopped）、作成成功直後のクラッシュは`create`の冪等契約で次tickが回復、削除失敗=failed経路（人間が原因解消→retryで再試行）。孤児worktreeは生じない（アーカイブはworktree削除成功/既不在の後にのみ行われ、workspace未記録でも決定的導出で再特定される） |
| UC-flow-005 | F5: runディレクトリの生涯(attempt蓄積とgc) | spec/flows/index.md#f5-runディレクトリの生涯attempt-蓄積と-gc | tick手続きAの`prepare_attempt`（および手続きC/abortの`write_invalidation_marker`による不在時作成）が起点。作成（tick）→pid/starttime/exit/ログの書き込み（wrapper）→参照（tickの観測・判定、show、判定コマンドのRUN_DIR）→gc削除（tick手続きE、`run_retention`設定時のみ）。終端はgcによる削除（attempt全削除後に親ディレクトリも削除）、`run_retention`未設定なら永続（クリーンアップでも削除しない、requirements§9.2）。削除失敗はスキップ・報告・次tick再試行（カウンタ消費なし）。保護規則：現役タスクの現在attempt（launching記録以外で不変）・stoppedの全attempt・Corruptタスクの全attemptは期間によらず削除しない。アーカイブ済み・孤児は保護しない。`attempt-<n>`形式外のエントリには触れない |
| UC-flow-006 | F6: ブランチの生涯 | spec/flows/index.md#f6-ブランチの生涯 | `WorktreeManager::create`（baseから分岐）が起点。作成（tick）→エージェントのコミット蓄積（ツール関知外）→worktree削除後も残存（成果物、requirements§9.1）→回収（マージ・PR）・削除（人間）。終端は人間による削除（ツールはブランチのライフサイクルに関与しない）。コミットのないブランチも異常ではない（cleanup.md）。回収前のユーザーによる削除は成果の喪失（ツールに復元手段なし、ログのみ残る） |
| UC-flow-007 | F7: タスクファイルの生涯 | spec/flows/index.md#f7-タスクファイルの生涯 | RegisterTaskの`create`が起点。作成（CLI）→アトミック置換による更新（tick/CLI、全遷移）→アーカイブ移動（tick手続きB）→参照のみ（`ls --all`/show）。終端はアーカイブ（以降tick走査対象外、abort/retry/set-statusは拒否）。アーカイブ済みファイルの整理は将来機能（スコープ外）。移動失敗はfailed経路（再試行はアーカイブ移動から実質再開）。破損（Corrupt）は全コマンドが書き込みを拒否し修復は人間の直接編集（ロック外のためtick停止中に行う）。スナップショットのみ破損（SnapshotUnreadable）はabort/retry/show/lsと再通知のみ可能でtickの進行からは除外（修復までの滞留） |
| UC-flow-008 | F8: ポーリング循環(skippedループ) | spec/flows/index.md#f8-ポーリング循環skipped-ループ | 循環`next`を持つワークフローでの登録（例watch→fix→watch、ADR-010）が起点。watch実行→判定コマンドがskipped→pending復帰（同一ステータス・新attempt、周期はtick間隔に律速）→…→completed→fixへ遷移→completed→watchへ循環（すべてtick）。終端はワークフロー上の終端に到達しない、出口は人間のabort（→stopped→set-statusでCleanupへ）または失敗経路のstoppedのみ。一過性失敗はfailed→リトライ、completed/skipped確定で連続カウンタがリセットされ蓄積しない（ADR-009）。周回ごとのattempt蓄積はF5のgcで対処（`run_retention`設定を推奨）。止め忘れはチェック実行と蓄積が続く（ツールは自動停止しない） |
| UC-task-006 | 共通事項: ホーム解決・config読み込み・ロック方針・parse境界 | spec/usecases/task.md#共通事項 | 全ユースケースが実行前にグローバルホームを解決(フラグ > `PULSEN_HOME` > 既定 `~/.pulsen/`)し `ConfigStore::load` を呼ぶ。`NotFound`/`Invalid`/`Io` は状態を変更せず非0終了。状態を変更するユースケース(RegisterTask/RetryTask/SetTaskStatus/Tick/AbortTask)は `ExclusiveLock::try_acquire` を取得し、競合(`Ok(None)`)は「別の操作が実行中」として非0(Tickのみ例外的に0でスキップ)、`Err(LockError)` も非0。ListTasks/ShowTask/RunWrapper はロックを取得しない。CLI引数の文字列→ドメイン型の変換は各ユースケースの入力境界で一度だけ行う |
