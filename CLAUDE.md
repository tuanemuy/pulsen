# エージェント向けガイド

AIエージェントタスクの汎用スケジューラー。

## 技術方針

- 言語はRust。安定版ツールチェーンを前提とし、`cargo fmt` / `cargo clippy` を通る状態を保つ。
- 特定のOSに依存しない（Linux / macOS / Windows）。OS依存の処理（プロセス同定・デタッチ起動・ロックなど）は、アダプター層に隔離する。
- アトミック性・排他が要る操作（アトミック置換・排他ロック）は共通のユーティリティに集約し、個別に再実装しない。

## アーキテクチャ: ヘキサゴナル

ドメインを中心に置き、依存は常に外側から内側へ向ける。

- ドメイン層: タスク・実行状態・ワークフローなどのモデルと遷移ロジック。外部クレートやI/Oに依存しない。
- ポート: ドメインが外界に要求する操作を trait として定義する（例: 状態ファイルの読み書き、プロセスの起動・観測、worktree操作、時刻、通知）。
- アダプター: ポートの実装。ファイルシステム、git、プロセス、`/proc` など環境依存の詳細はすべてここに閉じ込める。
- アプリケーション層: tickやCLI操作のユースケース。ポート越しに観測し、ドメインに判断させ、ポート越しに実行する。

テストでは実アダプターを差し替えられることを設計の健全性の指標とする。ドメインとユースケースのテストにファイルシステムや実プロセスが必要になったら、境界を見直す。

## 関数型ドメインモデリング

- 不正な状態を型で表現不能にする。OR関係はデータ付き enum、AND関係は struct。
- プリミティブは意味のある型でラップする。newtype（`struct TaskId(String)`）を使う。
- 検証は境界で一度だけ行う（parse, don't validate）。フィールドを非公開にし、`fn parse(...) -> Result<Self, _>` 経由で生成する。
- エラーは値として返す。ドメインエラーは enum で列挙して `Result` に載せ、パニックは不変条件違反にのみ使う。
- 分岐は網羅する。`match` でワイルドカード（`_`）を避ける。
- 状態遷移は型を変える純粋関数で表す。判断と副作用の実行を分離する。
- 値はイミュータブルに扱う。更新は `self` を消費して新しい値を返す。
- ドメイン型は共有可変状態を持たない自己完結した値とする。所有データのみで構成し、`Send` を満たす。
- 冪等性を前提に設計する。tickは永続化された事実から常に同じ判断を再導出できること。

## テスト方針

- テストは振る舞いを表す。仕様の言葉で名付け、実装の内部構造に依存させない。
- ドメインの遷移ロジックはユニットテストで網羅する。クラッシュ復旧・PID再利用・判定の冪等性など、specが明示するエッジケースを落とし込む。
- コードにもテストにも、指摘への弁明や修正の経緯を残さない。残すのは現在の形が成り立つ理由（why / why not）だけ。
